mailove (2.8) unstable; urgency=medium

  * A new spam rule: mail from a top-level domain your own sent mail never
    goes to scores slightly higher. The profile is built from the recipients
    of your own Sent mail, per account — someone writing to .sk and .com is
    not expecting first contact from .top — and it only ever corroborates:
    on its own it cannot mark anything, and a domain you already have a
    history with is never judged by the country it is in. Silent until
    enough sent mail exists to describe a habit (spam/tldMinSample).
  * Every spam rule's weight is now a setting, [spamrules] in advanced.conf:
    one key per rule id as printed in the "Why?" tooltip, so a rule that
    misfires on your mail can be found from the message it misfired on and
    turned down, or off with 0 — which removes it from the tooltip too.
    Changes apply to mail scored from now on; stored verdicts keep the score
    and the explanation they were given.
  * The advanced-settings groups are ordered alphabetically and each carries
    a heading and a plain-language explainer — in the reference beside the
    editor and as comments in the seeded file alike.
  * The spam threshold can no longer be set to 0, which would have marked
    every message in the mailbox: a message no rule fires on scores exactly 0.
  * Sender pictures are fetched at twice the size they are shown at, so they
    stay sharp on HiDPI screens. A few KB more, once per address per year.
  * New spam rules for mail sent through hacked websites, adapted from
    kawaiipantsu/spamassassin-rules: PHP code existing only in memory
    (php-eval-source), scripts mailing out of a CMS tree —
    WordPress or Joomla (php-cms-origin), a years-unmaintained PHPMailer
    (vulnerable-mailer), and .php links fed random tokens (hacked-php-url).
    An ordinary shop's PHP-sent receipt stays well below the threshold.
  * The link rules (link-text-mismatch, url-*) now answer to [spamrules]
    like every other rule; previously their weights were fixed.
  * Microsoft's compauth verdict (composite authentication, stamped by
    Microsoft 365) now counts: compauth=fail scores like any other
    authentication failure — including revoking the known-correspondent
    exemption for the spoofs of DMARC-less domains it exists to catch — and
    compauth=pass like a pass. The account's authentication checkbox
    remains the master switch; per-method spam/trustSpf, trustDkim,
    trustDmarc, trustArc and trustCompauth keys in advanced.conf drop one
    method's verdicts without giving up the rest (SPF broken by forwarding,
    a noisy Microsoft tenant). A distrusted method disappears from the
    message's authentication badge too, so what is shown is always what
    was counted; compauth appears there alongside the others.
  * The server-verdict rules read more providers' headers: Barracuda
    (X-Barracuda-Spam-Status), Proofpoint incl. iCloud
    (X-Proofpoint-Spam-Details), Yandex, Google Workspace (X-Gm-Spam,
    X-Gm-Phishy) and GMX/Web.de (X-UI-Filterresults). All feed the existing
    upstream rules and their [spamrules] weights; the forgery check (header
    must be added in transit) applies unchanged.
  * spf=softfail is scored apart from an outright failure: its own
    lower-weighted rule (auth-softfail), never revoking the known-sender
    exemption — forwarded mail is what softfail looks like — and shown in
    orange rather than red in the authentication badge.
  * The message viewer's authentication badges speak one language. The
    server's verdicts read as glyph-first pairs — SPF / DKIM / DMARC / ARC /
    COMPAUTH with green, orange or red check marks, a softfail orange rather
    than red, and several signatures for one method kept as "✓, ✗" so the
    comma still says there were two. Mailove's own DKIM and ARC verification
    stands at full size above them, likewise glyph-first.
  * Those badges explain themselves on hover: each tooltip is one block per
    method — the acronym in bold, a plain question ("SPF: Is the sending
    server allowed to send for this domain?"), the answer as a bold word
    (Yes / No / Warning / Not checked), and directly beneath it that
    method's own slice of the raw header as evidence.
  * The server's DKIM verdict is no longer shown beside mailove's own: two
    DKIM answers side by side read as a contradiction when they are really
    one check knowing more than the other (alignment, not just validity).
    The server's DKIM evidence moved into the tooltip of the badge that
    stays.
  * Authentication wording, tidied: every badge names the check it speaks
    for ("Modified after DKIM signing", "Modified after OpenPGP signing"),
    a signature from the wrong domain names both domains rather than saying
    only that they differ, and every string in the row starts with a
    capital.
  * The badge row no longer shifts as messages change: its labels share a
    baseline, so a taller glyph in one of them cannot nudge its neighbours
    up or down.
  * Fixed: the ARC exemption never actually applied — arc=pass was not
    extracted from Authentication-Results, so mailing-list mail whose
    SPF/DKIM broke in transit could be wrongly marked. The parsing now
    lives in spamheuristics.cpp under test.
  * More hacked-website coverage: links into WordPress code directories
    (hacked-wordpress-link, from martinschaible/rspamd-rules), scripts
    mailing from /.well-known/ or the PHPUnit RCE path, and a URL-shortener
    list grown from 16 to 116 services. Newsletter media links under
    wp-content/uploads are deliberately not matched.

 -- Daniel Duris <dusoft@staznosti.sk>  Mon, 17 Aug 2026 12:00:00 +0200

mailove (2.7) unstable; urgency=medium

  * Advanced settings, under Settings -> Advanced: everything with no widget of
    its own — sync pacing, connection counts, protocol timeouts, spam weights,
    cache thresholds, database timeouts — edited as plain INI in
    ~/.config/mailove/advanced.conf, with a searchable reference beside the
    editor generated from the same schema the values are read through.
  * A value outside its range is corrected on save and an unknown key ignored
    with a warning; neither stops the rest of the file from applying. Only a
    syntax error is refused, and it names the line it is on. Comments, blank
    lines and ordering survive exactly as typed.
  * Keys that only take effect on restart say so, under the Apply button, and
    keep saying so after the save rather than the moment before it.
  * Emptying the file removes it and restores stock behaviour; blank lines at
    the top and bottom are trimmed, the ones between sections left alone.
  * No secret is ever written to that file. An OAuth client secret typed there
    is moved into the system wallet on save and the line rewritten to @wallet
    — including for a file edited by hand outside the client, which is swept
    at startup. Nothing in the client can read a secret back out of the file.
  * Sender pictures (Gravatar), off by default: asking gravatar.com for a
    picture tells gravatar.com that the address was seen, so nothing is
    requested or cached until avatars/enabled says so. The address never
    leaves the machine — what goes out is its SHA-256 — and answers are kept
    for a year, misses for a month.
  * How long a message must stay open before it counts as read is now a number
    of seconds rather than on or off, decimals included. 0 leaves it unread
    until marked read by hand, and moving through a list no longer marks
    everything on the way past.
  * The message context menu alternates instead of offering the command that
    does nothing: Mark read or Mark unread by the message's own state, Mark as
    spam or Not spam by its mark — and always Not spam in the junk folder,
    where a message put there by a server-side rule carries no local score.
  * DKIM key records are cached for at least half an hour rather than five
    minutes. Key rotation publishes a new selector, so a stale record for the
    old one is never asked for again.

 -- Daniel Duris <dusoft@staznosti.sk>  Sun, 16 Aug 2026 00:00:00 +0200

mailove (2.6) unstable; urgency=medium

  * Forward as attachment: forwards the message being read as a message/rfc822
    attachment — the original bytes untouched, every image, attachment and
    signature intact and nothing re-rendered. In the message context menu, for
    a single message.
  * Copy as Markdown: select part of an HTML message and copy it as Markdown
    rather than as flattened plain text.
  * HTML to plain text rewritten: layout tables, lists and block structure now
    survive the conversion instead of collapsing, and runs of blank lines are
    capped. Used everywhere text is rendered — the text view, plain-text
    quoting, and the body the spam rules read.
  * Reply and forward quotes stream into the composer a batch per event-loop
    tick, so the composer opens instantly on a newsletter-sized message and
    stays responsive while the quote fills in. Send and Save as draft flush
    what is still pending first.
  * Remote content in a quote is neutralized on a parsed document rather than
    with regexes over markup: remote images and backgrounds go, only data:,
    file: and cid: references survive. Where remote content was allowed, the
    images are prefetched and embedded as cid: parts at send time, per image,
    without the send ever waiting on the network.
  * The composer's modified check compares the document revision instead of
    serializing the whole body to HTML, which blocked the GUI for the better
    part of a second on a large message.
  * Spam scoring decodes RFC 2047 headers before judging them. Encoded-word
    subjects were invisible to the homoglyph and confusable rules — precisely
    the mail those rules exist for.
  * List mail is recognised where the link rules read it, and a rule that
    fired only on ordinary mail was dropped rather than weakened.

 -- Daniel Duris <dusoft@staznosti.sk>  Sat, 15 Aug 2026 00:00:00 +0200

mailove (2.5) unstable; urgency=medium

  * Spam detection rewritten around the message rather than the X-Spam headers:
    brand impersonation in the sender name, sender-name and URL homograph and
    zero-width tricks, credential-trick and brand-lookalike links, password
    forms in the body, web pages and password-protected archives sent as
    attachments, and the business-email-compromise shape of a corporate sender
    with a freemail Reply-To.
  * Attachment and link rules now actually run. They existed but the client
    only ever scored headers, so nothing that needed a body could fire; a
    message is scored again once its body is in hand.
  * ARC is read, so mailing lists are no longer marked. A list breaks SPF and
    DKIM by design and ARC carries the verdict from before it did.
  * Three rules that fired on ordinary mail are gone or weakened, measured
    against a real mailbox: Message-ID and Return-Path naming a different
    domain than From (61% and 6% of ordinary mail — every mailing platform
    does both, and SPF already authorises it) and Reply-To mismatches.
  * A brand cannot impersonate itself: links to a sender's own asset and
    shortener domains are no longer read as lookalikes.
  * The red ! now means spam and only spam. Hovering lists every rule that
    fired with its weight and the total the verdict was made from.
  * Everything in the junk folder is marked, including mail filed there before
    any of this existed.
  * Scoring runs in the inbox and the junk folder only — never Sent, Drafts or
    Trash, and never in imported archives, whose mail lacks the headers the
    rules assume and would be marked for being old.
  * Taking a message out of the junk folder allowlists the sender, whether by
    the Not-spam action or by moving it; Not spam also puts it back in the
    inbox instead of leaving it buried.
  * Sent and Drafts show To instead of From, sort by it, and search it.
  * A one-time cache migration fills that column in from mail already on disk,
    with a progress dialog. Nothing is re-downloaded.
  * The message list no longer waits for an unrelated event to notice that the
    open folder changed.
  * A clipboard image that cannot be pasted into a message says why instead of
    doing nothing.

 -- Daniel Duris <dusoft@staznosti.sk>  Fri, 15 Aug 2026 00:00:00 +0200

mailove (2.4) unstable; urgency=medium

  * Ctrl+V puts a picture from the clipboard into the message where the
    cursor is, instead of only being able to attach one as a file. It travels
    with the message as an inline part, survives saving and reopening a draft,
    and is sent as an attachment on plain-text accounts, which have no body
    that can reference it.
  * New Layout setting: the message preview can sit below the message list as
    before, or beside it. Each layout remembers its own divider position, and
    the folder pane width is now remembered too.
  * In the beside layout the list shows each message on two lines, sender and
    date above subject, separated by a hairline rule; sorting moves to a
    button next to the search field and stays in step with the column
    headers of the other layout.
  * Search works offline and in imported archives instead of refusing with
    "Not connected" — the local index is the search when no server can be
    reached.
  * A search that found nothing no longer leaves a filter behind that hid the
    results of the next one, and clearing a search offline restores the
    folder instead of leaving the hits on screen.
  * Typing in the search field no longer resets the message list on every
    keystroke.
  * Settings help texts rewritten throughout: shorter, and all at one size.
    The JMAP note no longer claims that marking read, moving, deleting and
    sending are unimplemented — they are.
  * Simplified application icon.

 -- Daniel Duris <dusoft@staznosti.sk>  Thu, 13 Aug 2026 12:00:00 +0200

mailove (2.3) unstable; urgency=medium

  * Renamed from Mailo to Mailove. The binary, the desktop entry, the icon
    and the internal URL scheme all follow the new name.
  * Because the application name changed, settings and cached mail now live
    under ~/.config/mailove/ and ~/.local/share/mailove/. Nothing is migrated
    from the old mailo directories: accounts must be added again and each
    one re-syncs from the server on first use. The old directories are left
    untouched and can be removed by hand.

 -- Daniel Duris <dusoft@staznosti.sk>  Wed, 12 Aug 2026 12:00:00 +0200

mailove (2.2) unstable; urgency=medium

  * Better spam identification.
  * UI and UX fixes against the 20 ms rule: message pages are prefetched
    ahead of the scroll, and holding Page Down no longer degrades over a
    long list.
  * Read-only folders now fail cleanly instead of surfacing a rejected
    STORE.
  * Background workers run at low priority, so they no longer compete with
    the GUI thread.
  * Logging changed: switched off now means a silent console.
  * Added a GUI-thread stall detector and paging timings for diagnostics,
    both reported only when logging is enabled.
  * Groundwork for Gravatar sender icons is present but inert: not built,
    not reachable from the UI, and likely to be removed — an avatar lookup
    tells a third party who is mailing you, which is not a switch worth
    offering even off by default.

 -- Daniel Duris <dusoft@staznosti.sk>  Mon, 10 Aug 2026 21:24:40 +0200

mailove (2.1) unstable; urgency=medium

  * Accounts other than the open one now sync their mail in the background,
    not just their unread counts. Each refresh walks that account's folders —
    the inbox first, the rest after — and caches everything that arrived since
    the last visit, so switching to an account shows the mail its unread badge
    already promised instead of fetching it then.
  * Packaging: the .deb and the AppImage are both written to the project root,
    named with the version, and CPack's staging directory is cleaned up after
    itself. `cmake --build build --target packages` builds both.
  * The AppImage build no longer requires FUSE on the build machine; where the
    kernel device is missing the packaging tools run in extract-and-run mode.

 -- Daniel Duris <dusoft@staznosti.sk>  Fri, 07 Aug 2026 12:00:00 +0200

mailove (2.0) unstable; urgency=medium

  * JMAP support (RFC 8620/8621). An account now chooses its protocol: IMAP
    as before, or JMAP, which discovers its own endpoints from the address
    and authenticates with an API token or a password. Both kinds of account
    can be configured at once.
  * Mail protocols sit behind one interface, so which mailbox is the trash,
    when to backfill and what a header means are answered the same way
    whichever protocol replied. Push arrives as IMAP IDLE or JMAP
    EventSource and is handled identically.
  * Fixed: migrating an account from a pre-1.0 install silently reset its
    IMAP port to 993, its security to SSL/TLS and lost its SMTP settings.
    The values were read from the wrong settings scope.
  * Fixed: after the wallet lookup finished, a connection requested while it
    was still in flight was never resumed, so the client could sit offline
    with a perfectly good account configured.
  * Fixed: a DKIM or OpenPGP verdict on a message rebuilt from the cache
    could be attributed to whichever message was on screen when a delayed
    retry landed, reporting cache staleness as tampering.
  * Quieter status bar: breadcrumbs that only repeated what the window
    already showed (an empty folder, a link opening in the browser) are gone
    from the status line and kept in the debug log.
  * Internals: the mail controller is split into an account store, a sync
    engine, a cache-maintenance scheduler, a message presenter and a
    signature verifier, each testable on its own. The test suite grew to
    twelve binaries covering the cache, account storage and the attachment
    split/restore pair.

 -- Daniel Duris <dusoft@staznosti.sk>  Wed, 05 Aug 2026 12:00:00 +0200

mailove (1.5) unstable; urgency=medium

  * Unread counts in the folder pane: a pill on every folder, blue on the
    inbox, and an outline on a collapsed folder standing in for the unread
    sitting in subfolders folded away beneath it.
  * Other accounts are checked for new mail on the refresh schedule — connect,
    STATUS, disconnect — so their unread counts move without switching to them.
  * Spam retention (Settings, General): mail sent longer ago than the chosen
    number of days is removed from the spam folder automatically, going by the
    date the message carries. "Skip trash for spam" (on by default) deletes it
    outright rather than filing it in the trash to be cleared out twice.
  * Folders can be dragged onto another folder to reparent them, or onto the
    account name to move them to the top level. Imported archives included.
  * Rename folder in the folder context menu. Where the protocol forbids it —
    INBOX, and the special-use mailboxes a server recreates — the menu says so
    in place of the command.
  * Message context menu, starting with Mark unread. A message marked unread
    while open stays open and stays unread; it is marked read again the next
    time it is opened.
  * A "No label" key that clears any colour label, alongside the five.
  * Junk is called spam throughout the interface, except where a folder is
    named otherwise by the server.
  * Fixes: an account switch no longer carries the previous account's folder
    delimiter, which could rewrite an imported archive's folder paths; folder
    trees keep parents directly above their children; folders in the sidebar
    could not be dragged at all.

 -- Daniel Duris <dusoft@staznosti.sk>  Tue, 04 Aug 2026 12:00:00 +0200

mailove (1.1) unstable; urgency=medium

  * Compose, Settings and opened messages are tabs in the main window rather
    than separate windows; the composer can still be a window of its own
    (Settings, Look and feel).
  * Accounts keep the e-mail address they send from separately from the login
    name, so the two may differ, and can carry a full name and organization.
  * Imported mail is its own account type, without server settings.
  * Accounts can be reordered by dragging them in the folder pane.
  * Message list: Page Up/Down, Home/End, Enter to open, Ctrl+W to close a
    tab, and the keyboard follows the folder you open.
  * Much faster paging through large folders, and background updates no
    longer slow down as more of a folder is loaded.
  * Reclaiming disk space no longer freezes the window while it starts.
  * File pickers are the desktop's own, with its colours and Places sidebar.

 -- Daniel Duris <dusoft@staznosti.sk>  Sun, 02 Aug 2026 12:00:00 +0200

mailove (1.0) unstable; urgency=medium

  * Attachments are stored as content-addressed files outside the database,
    deduplicated and zstd-compressed; existing caches are migrated in place.
  * Cache maintenance in Settings: reported size, and an explicit vacuum to
    return free space to the filesystem.
  * Gmail's All Mail archive is excluded from caching and hidden, removing
    the duplicate copy of every message.
  * Compose: Save as draft, resuming a draft from the Drafts folder, and a
    confirmation before a message is discarded.
  * Shortcuts act anywhere in the window rather than only in the message list.
  * Settings dialog is resizable and remembers its size.
  * Faster start-up, and folder and message selection no longer lost to
    background refreshes.

 -- Daniel Duris <dusoft@staznosti.sk>  Fri, 31 Jul 2026 12:00:00 +0200

mailove (0.9) unstable; urgency=medium

  * Initial release.

 -- Daniel Duris <dusoft@staznosti.sk>  Mon, 20 Jul 2026 12:00:00 +0200
